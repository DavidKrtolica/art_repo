import PageHeader from '../PageHeader'

import { tertiary } from '../../utils/colors'

const Auth = () => {
  return (
    <>
      <PageHeader title="Auth" color={tertiary.main} />
    </>
  )
}

export default Auth
